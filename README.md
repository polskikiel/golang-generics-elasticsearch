# Golang generics elasticsearch

This project provides generic functions for indexing, updating, deleting or droping the elastic search indexes.

It is using [this](https://github.com/olivere/elastic) library.